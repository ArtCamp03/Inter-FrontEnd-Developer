// metodo filter retorna apenas os item que passaram pelo filtro
// Ex:

const frutas = ['maca roxa','maca verde','laranja', 'morango','banana','melao'];
frutas.filter((fruta) => fruta.includes('maca'));