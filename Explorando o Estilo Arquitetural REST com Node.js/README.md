# Aplicaçao node com express mo estilo arquitetural REST

// instalar dependencias necessarias:
npm install

// Type Script
npm install -g typescript

tsc --init

npm install --save-dev typescript

npm install --save-dev @types/node